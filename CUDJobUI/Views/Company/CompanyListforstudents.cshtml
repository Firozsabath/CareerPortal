@model IEnumerable<CudJobUI.ViewModels.CompanyViewModel>
@{
    ViewData["Title"] = "CompanyListforstudents";
    Layout = "~/Views/Shared/_LayoutStudent.cshtml";
    ViewData["ShowButtons"] = false;
    //var profilePic = "";
}

<div>
    <partial name="~/Views/Shared/Partials/_CompanyListPartial.cshtml" model="Model" view-data="ViewData" />
</div>

@*<div class="m-5 stdCompanydt">
    <h4 class="Sectiontittle shortheading mb-3">Companies List</h4>
    @foreach (var company in Model)
    {
        <div class="card mb-3">
            <div class="card-body">
                <div class="row">
                    <div class="col col-md-1">                       

                        @if (company.Companies.profileImgpath == null && company.Companies.profileImgpath != string.Empty)
                        {
                            <img class="rounded-circle w-100" alt="avatar1" src="https://www.cud.ac.ae/sites/default/files/personnel/2021/ms-fanan-al-madi.jpg" />
                        }
                        else
                        {                           
                           profilePic = System.IO.Path.GetFileName(company.Companies.profileImgpath);                             
                            <img class="rounded-circle w-100" alt="avatar1" src="~/Images/Company/Profile/@profilePic" />                            
                        }

                    </div>
                    <div class="col col-md-9 pt-2">
                        <h4 class="mb-2">@company.Companies.CompanyName</h4>
                        <ul class="d-md-flex flex-wrap ml-md-4">
                            <li class="mr-md-4"><i class="fas fa-building mr-2"></i> @company.Companycategory.Category</li>
                            <li class="mr-md-4"><i class="fas fa-users mr-2"></i>@company.Companies.CompanyStrength</li>
                            <li class="mr-md-4"><a target="_blank" href="@company.Companies.Website"><i class="fas fa-code mr-2"></i> @company.Companies.Website</a></li>
                        </ul>
                    </div>
                    <div class="col-md-2 mt-4">
                        <a href="@Url.Action("JobsforStudentsByCompany", "Job", new { id = company.Companies.CompanyID })" class="btn btn-block btn-light btn-custom">
                            <span><i class="fas fa-user-md me-2"></i></span> <span>View Jobs</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    }

</div>*@

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('#example').DataTable();
        });
    </script>
}

