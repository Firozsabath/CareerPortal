@model IEnumerable<CudJobUI.ViewModels.JobApplyDetailsBystudent>
@{
    ViewData["Title"] = "JobsByStudent";  
}

<div class="container-fluid p-4">
    @*<div class="card">
        <div class="card-header">
            <h4 class="Sectiontittle shortheading">Jobs Applied By Student</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive-md">
                <table id="example" class="table table-striped table-bordered text-center tablelist">
                    <thead>
                        <tr>
                            <th>
                                Position Tittle
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Company.CompanyName)
                            </th>
                            <th>
                                Application Status
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.JobDetails.LastApplyDate)
                            </th>
                            <th>Application Date</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>                                    
                                    @Html.ActionLink(item.JobDetails.Title, "Details", "Job", new { id = item.JobDetails.Id })
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Company.CompanyName)
                                </td>
                                <td>
                                    <span class="text-success">@Html.DisplayFor(modelItem => item.JobStatus.Status)</span>
                                    <span class="d-inline-block" tabindex="0" data-toggle="tooltip" title="@item.AppliedJob.Description">
                                        <i class="fa fa-question-circle" aria-hidden="true"></i>
                                    </span>
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.JobDetails.LastApplyDate)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.AppliedJob.AppliedDate)
                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>*@

</div>


<div>
    <partial name="_AppliedJobsPartial" model="Model" />
</div>

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $('[data-toggle="tooltip"]').tooltip();
            $('#example').DataTable({
                dom: 'Bfrtip',
                buttons: [
                    'copyHtml5',
                    'excelHtml5',
                    'csvHtml5',
                    'pdfHtml5', 'print'
                ]
            });
        });
    </script>
}
